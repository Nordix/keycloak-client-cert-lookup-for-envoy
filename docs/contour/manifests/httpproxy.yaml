apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: keycloak
spec:
  virtualhost:
    fqdn: keycloak.127.0.0.1.nip.io
    tls:
      secretName: server-cert
      clientValidation:
        caSecret: client-ca
        optionalClientCertificate: true
        forwardClientCertificate:
          cert: true
  routes:
    - services:
        - name: keycloak
          port: 8443
          protocol: tls
          validation:
            caSecret: server-ca
            subjectName: keycloak
